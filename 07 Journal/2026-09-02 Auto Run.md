# 🤖 Auto Run — 2026-09-02

> Written by an unattended cloud routine ([[Autonomous Lane — Design]]). One note per day;
> each fire appends a section headed with its local time.

## 09:18 (Lisbon) — first fire of the day

### ⚠️ Needs you

- **The lane opened its first PR. [#66](https://github.com/techattackteam/TechEngine/pull/66)
  is S5-P2, branch `S5-P2/log-level-fallback`, and it is waiting on your review.** The PR path
  was the one thing [[Autonomous Lane — Design]] § *State* still listed as unproven; it is
  proven now, end to end, and the day's one-PR budget is spent.
- **CI had not settled when this fire ended.** `clang-format` and `no-code scope` are green,
  `diff coverage` was still running and the eight build legs had not started — they are gated
  behind the two cheap checks. **Nothing here claims the PR is green.** The next fire reads it.
- **[[Known Issues]] D1 is deleted**, in the same push as this note. If you reject #66, the
  entry has to come back — it is not recoverable by re-running anything.
- **One judgement call in #66 worth your eye.** D1 asked for the fallback *and* a test, and the
  test it asked for covers the linked path, not the fallback. **Nothing in the tree includes
  `Log.hpp` without linking `base`**, which is D1's own trigger condition, so the branch I
  changed is not reached by any TU today and no case can reach it either without a TU that
  undefines the gate on purpose. I did not write that TU. Detail below.
- **A pre-existing build break, found while verifying, filed not fixed.**
  `-DTE_LOG_ACTIVE_LEVEL=3` fails the Linux leg outright. `#prio/low` on [[Backlog]].

### What ran

| | |
|---|---|
| **Card** | **S5-P2** · 🤖 [[Known Issues]] D1's fallback fix · P3 · 🤖 Auto |
| **Outcome** | Shipped. Both `done:` clauses met, D1 deleted, card ready to close on merge. |
| **PR** | [#66](https://github.com/techattackteam/TechEngine/pull/66), branch `S5-P2/log-level-fallback`. **Never merged, and auto-merge was never enabled.** |
| **CI** | **Not settled.** `clang-format` ✅ · `no-code scope` ✅ · `diff coverage` still running · build legs not yet started. |
| **Local build** | `linux-debug` configure + build + `ctest` all green, **210/210 passing**. |

### What changed

**Engine, on the branch — 2 files, +23 lines, no deletions.**

- `engine/base/include/TechEngine/base/diagnostics/Log.hpp:16-25` — the fallback now branches on
  `NDEBUG` instead of assuming Trace, per D1's written fix.
- `engine/base/tests/diagnostics/LogTests.cpp` — one case, *the library gates at the level this
  TU compiled*, plus the `<source_location>` include it needs.

**Vault, on `master`.**

- [[Known Issues]] — **D1 deleted.** Nothing else in the file moved; D3 and D4 are untouched.
- [[Backlog]] § *base* — one entry added, the `-DTE_LOG_ACTIVE_LEVEL` build break.

### The fix, and the one thing it cannot prove

`Log.hpp` fell back to `TRACE` whenever the define was absent, so a TU including the header
without linking `base` compiles every Trace and Debug call site into a Release build. It now
follows `NDEBUG`, which is the only config signal such a TU has. `RelWithDebInfo` therefore
lands on Info where the link would have given it Debug — the quieter side, which is D1's own
reasoning and not a new decision.

**The test is deliberately not a test of that branch.** D1 asked for a case "mirroring
`AssertTests.cpp`'s config-table case (it compares the library's compiled view against the test
TU's)", and that is what shipped: the library filters on the value it was compiled with
(`Log.cpp:347`) and the macros on the value the including TU sees, so dispatching past the
macros at all six levels and counting survivors pins the two together.

| | Covered by the new case? |
|---|---|
| The define reaches the library and the TU, and they agree | **Yes.** This is the shape `TE_ASSERT_DEV` already had and this gate did not. |
| The fallback's own `NDEBUG` branch | **No.** Nothing in the tree reaches it. |

Covering the second row needs a TU that `#undef`s the gate before including the header, in its
own file because the undef would poison every other case in `LogTests.cpp`. That is a second
source file and a `CMakeLists` entry for a branch no production TU reaches, so I stopped and
left it to you rather than widening an unattended diff.

**I did check the case is not a tautology.** Reconfigured with `-DTE_LOG_ACTIVE_LEVEL=3`, the
case still passes and its expected count moves with the define, so it tracks the gate rather
than a hardcoded number.

### Findings

One [[Backlog]] entry added.

**1. Setting `TE_LOG_ACTIVE_LEVEL` above Info fails the build** (`#prio/low`, § *base*).
`cmake --preset linux-debug -DTE_LOG_ACTIVE_LEVEL=3` dies at
`engine/base/tests/math/MathFormatTests.cpp:47`: `position` is read only by a `TE_LOGGER_INFO`
that the gate then compiles out, `TE_LOG_PRIVATE_DISCARD` drops its arguments unevaluated by
design, and `-Werror -Wunused-variable` does the rest. The cache variable is offered at
`engine/base/CMakeLists.txt:18` and **no CI leg sets it**, so nothing catches this. Pre-existing
and unrelated to #66 — it reproduces on `master` by inspection of the same two files, and the
default-configured build is green.

### Environment

Nothing needed repair. The `docs/` symlink and `wayland-scanner` were both present on arrival.
Step 1's re-anchor ran on both repos; both report `is-shallow-repository: false` afterwards.
Yesterday's Dashboard revert **has been fixed** — the file on `master` today carries the
Sprint 05 rows and the `01ed7a30` stamp, so that finding is closed and is not repeated here.

### Unverified

- **CI is not settled, and this report claims nothing about it.** Two cheap checks are green;
  the eight build legs had not started. A red leg is possible and the next fire reads it.
- **`linux-debug` only.** No Windows leg, no `linux-release`, no sanitizer leg was run. The
  `NDEBUG` half of the fallback is therefore not compiled by anything this run executed.
- **The fallback branch has no coverage at all**, by construction — see above. The fix is
  correct by inspection of six preprocessor lines, which is a weaker claim than a green test.
- **`diff coverage` at 85% is a required check and this diff is mostly a test file plus
  preprocessor lines.** I did not attempt to predict what it reports.
- **The finding's "pre-existing" claim was not tested against a clean `master` checkout.** It is
  read from the two files, neither of which #66 touches.

### Cost

| | |
|---|---|
| **CI** | ~16.1 billed minutes, the day's one code PR. |
| **Review owed** | ~15 min: #66 is 23 lines, and one call on whether the fallback branch is worth its own test TU. |

## 14:15 (Lisbon) — second fire, report-only

### ⚠️ Needs you

- **[#66](https://github.com/techattackteam/TechEngine/pull/66) is green. All 11 checks
  passed** — the eight build legs including **all four Windows legs and both sanitizers**,
  plus `clang-format`, `no-code scope` and `diff coverage`. `mergeable_state: clean`. The
  morning fire could only report two cheap checks; this is the settled answer. **It is waiting
  on your review and nothing else.**
- **I edited #66's PR body to strip an AI attribution footer.** The morning fire's PR body
  ended in `_Generated by [Claude Code](…)_`, which CLAUDE.md rule 10 forbids outright. The
  commit itself was clean — author, committer and message all Miguel, no trailer. **This will
  recur on every PR this lane opens** unless something stops it; see *Findings*.
- **Two [[File Access — Design]] drifts filed, not fixed.** #63 and #65 deleted the demo mount
  and the note still describes it in the present tense. Details below.
- **No new PR.** The day's one-PR budget went to #66 this morning, so this fire took
  report-only work, per the routine's own cap.

### What ran

| | |
|---|---|
| **Card** | None. A PR was already open for today, so no card was taken. |
| **Work** | Read #66's CI to settled; ran the freshness fallback over the two commits yesterday's check did not reach. |
| **Range checked** | `b627332..40f7171` — #64 (`4e3c6f7`) and #65 (`40f7171`). |
| **PR** | None opened. #66 was **not** merged and auto-merge was **not** enabled. |
| **Build** | Nothing compiled. No engine code changed this fire. |

### #66's CI, settled

Every required context reported. `matrix.context` is `skipped` by design — that is
[[B3 — Build & Testing Notes]]'s docs-only stand-in declining a code PR, not a gap.

| Leg | |
|---|---|
| `win-msvc Debug` · `win-msvc Release` · `win ASan` | ✅ |
| `linux-clang Debug` · `linux-clang Release` · `linux UBSan` · `linux TSan` | ✅ |
| `clang-format` · `no-code scope` · `diff coverage` | ✅ |

**This retires two of the morning report's *Unverified* lines.** The `NDEBUG` half of the
fallback is now compiled by the Release and Windows legs, and `diff coverage` — the one it
flagged as unpredictable on a diff that is mostly a test file — passed.

### Findings

Two [[Backlog]] entries added under § *platform*, and one thing fixed rather than filed.

**1. The lane's PR bodies carry an AI attribution footer** (fixed on #66; no entry filed).
CLAUDE.md rule 10 and the routine's step 7 both forbid it, and the footer is appended by the
harness rather than typed, so writing a clean body is not enough to prevent it. Editing the
body costs **zero CI minutes** — `ci.yml`'s `pull_request:` trigger takes the default
`[opened, synchronize, reopened]` and `edited` is not among them (`.github/workflows/ci.yml:40-46`),
which is why I fixed it in-flight rather than leaving it for you. **The mechanism is untouched
and the next lane PR will do it again.** Whether that becomes a card is your call — it is a
check-and-strip step in the routine, not engine work.

**2. § *The demo mount is throwaway* describes deleted code** (`#prio/medium`). The section
is present-tense about a mount through `TE_DEMO_ASSETS_DIR` with a `TODO(S3-T13)` in two
files. #63 deleted the demo body, #65 the define, the TODO and `engine/app/assets/`; neither
symbol appears anywhere under `engine/`, `apps/` or `cmake/` now. Nothing outside tests calls
`mount()` at all, so the section's "M3 replaces the whole mount set anyway" is wrong in the
other direction — there is no mount set. **Whoever writes M3's mount card reads this section
for prior art**, which is what makes it worth a `medium`.

**3. § *Wiring* names the wrong owner and the wrong field count** (`#prio/low`). The
composition root has been the `App` class since #63, not `run()` (`App.hpp:16-17`); by-value
is still correct. And "`EngineContext` has one field today" has been two since `jobs` landed
(`EngineContext.hpp:8-9`). The paragraph below it argues a service earns a field only when
something needs it through the context "and nothing does yet" — `jobs` already falsified that,
so this is a stale argument and not only a stale number.

### Checked and deliberately not filed

- **[[B3 — Build & Testing Notes]] § *What the tree costs today* names
  `core/tests/events/AssertCapture.hpp`**, which #64 moved to `tests/support/`. The section
  opens "Measured across the whole repo at engine `a0d1d1b3`", so it is a **dated snapshot**
  and a moved file does not falsify it. This is the same shape S5-P3 correctly left alone in
  the Accepted ADRs.
- **The Dashboard's *Current focus* row still reads 🔨 S5-T2**, which is `[x]` on
  [[Sprint Board]] since #64. The Dashboard's own header says it is updated during the weekly
  review, so being one card behind mid-sprint is its designed state. The weekend ceremony
  refreshes it.
- **S5-T2's shipped validation matches its design row exactly.** [[File Access — Design]]'s
  *Alias validation* row promises `mount()` rejects an empty alias, a `/` and a `:` with fatal
  checks; `MountTable.cpp:33-35` is those three `TE_CHECK`s. Reconciled, no action.

### Environment

Nothing needed repair. The `docs/` symlink and `wayland-scanner` were both present on arrival.
Step 1's re-anchor ran on both repos and both report `is-shallow-repository: false` afterwards,
so the commits read in this report resolve against a complete history.

### Unverified

- **Nothing was compiled this fire.** The CI result above is read from GitHub's check runs, not
  reproduced locally. No claim here rests on a local build.
- **The freshness check covered `b627332..40f7171` only** — two commits, against the artifacts
  that name what they touch ([[File Access — Design]], [[B3 — Build & Testing Notes]],
  [[Sprint Board]]). It is a spot-check at that depth, not a line-by-line audit.
- **The Dashboard stamp was not advanced**, and this range stays formally unstamped: only
  `/weekly-review` or `/sprint-plan` moves it. Read #64 and #65 as checked but unstamped.
- **The zero-CI-cost claim for the body edit is read from the workflow's trigger list**, not
  observed against the Actions tab afterwards.
- **Finding 1's "will recur" is inference** from the footer being harness-appended rather than
  authored. I did not test it by opening a second PR.

### Cost

| | |
|---|---|
| **CI** | 0 billed minutes. No PR opened, no engine commit, no re-run triggered. |
| **Review owed** | ~15 min, unchanged: #66 is green and the review is the only thing left on it. Two [[Backlog]] entries to skim, and one call on the attribution footer. |
