# 🤖 Auto Run — 2026-09-03

> Written by an unattended cloud routine ([[Autonomous Lane — Design]]). One note per day;
> each fire appends a section headed with its local time.

## 09:13 (Lisbon) — first fire of the day

### ⚠️ Needs you

- **[#66](https://github.com/techattackteam/TechEngine/pull/66) is still open, still green, and
  has now been waiting on your review for a full day.** Head is unmoved at `9e829c6`, all 11
  checks still report the same run from Sep 2, and `mergeable_state` is `clean`. It is the
  lane's first PR and merging it is the only thing left on S5-P2.
- **Two [[Backlog]] triggers had fired and nobody had marked them.** One of them fired **after**
  the entry was written, in the same day: #65 changed `engine/app/CMakeLists.txt` about five
  hours after the Sep-1 fire filed the `DEPS_PRIVATE platform` entry against that exact file,
  and the one-line fix did not ride along. Details below.
- **No PR, and the day's budget is untouched.** No 🤖 Auto card is takeable — S5-P2's work is
  sitting in #66 and S5-P3's remaining three sites need your call — so this fire took a
  [[Backlog]] trigger sweep, which is the eligible report-only job that was oldest.
- **The Auto lane will be empty again tomorrow** unless #66 merges and S5-P3 gets its call.
  That is the same *"an empty lane leaves a fire with no work by construction"* shape S5-P1
  recorded, and the next `/sprint-plan` is not until Sep 12-13.

### What ran

| | |
|---|---|
| **Card** | *No 🤖 Auto card takeable* — ran a [[Backlog]] trigger sweep |
| **Outcome** | Report only. Two triggers marked fired, one entry corrected, nothing else touched. |
| **PR** | None opened. #66 was read, **not** merged, and auto-merge was **not** enabled. |
| **CI** | #66 unchanged and green — 10 successes plus `matrix.context` skipped by design. |
| **Local build** | **Not run.** No engine code changed, so step 6 had nothing to prove. |

### Why a trigger sweep, and why not the freshness check

Step 4's fallback is a freshness check and it had nothing left to check. `origin/master` is
still `40f7171`, unmoved since Sep 1, and the whole `01ed7a30..40f7171` range has already been
covered: `01ed7a3..b627332` by the Sep-1 second fire and `b627332..40f7171` by the Sep-2 second
fire. Re-running it would re-derive both of those.

The trigger sweep is the other eligible report-only job and it was genuinely due. The last one
ran **2026-08-31**, and the [[Backlog]] has roughly doubled since — eleven entries were filed
across the Sep-1 and Sep-2 fires, and #64 and #65 both merged in between.

### What changed

- **[[Backlog]] only.** Two `Trigger:` lines marked fired (§ *app*, § *etc*), one entry in
  § *etc* corrected. No artifact was edited, no engine file was touched, and no branch was cut.

### Findings

Three, all inside [[Backlog]] itself. No new entry was needed.

**1. `engine/app` declares `platform` PRIVATE — trigger fired, and the change went past it**
(`#prio/low`, § *app*, marked). The entry's trigger was "the next `engine/app/CMakeLists.txt`
change". It was filed by the Sep-1 second fire at about 14:20 Lisbon, and **#65 changed that
file at 19:54 the same day**, deleting the `TE_DEMO_ASSETS_DIR` block. A one-line
`DEPS_PRIVATE` → `DEPS` fix would have cost that PR nothing. The citation survived the edit
intact: `DEPS_PRIVATE platform` is still `engine/app/CMakeLists.txt:6`, because the deleted
block sat below it.

**2. ADR-009's workflow amendment — trigger fired at Sprint 05 planning** (`#prio/medium`,
§ *etc*, marked). Its trigger reads "Sprint 05 planning, or the first workflow break that
reaches `master` green". Sprint 05 planning ran on **2026-08-30** and the entry was not pulled
and not marked. Whether grooming passed it over deliberately is not something this run can see,
so the entry now says exactly that and points at the next `/sprint-plan`.

**3. The `.gitattributes` entry's witness no longer exists** (`#prio/low`, § *etc*, corrected).
It was written against `engine/app/assets/demo.txt` getting CRLF on a Windows checkout. #65
deleted that file, and `git ls-files` now returns **no committed data asset of any kind** —
the only non-source files left are `.claude/settings.json` and `CMakePresets.json`. The concern
is sound and the trigger is still the right gate, so the entry is corrected rather than
dropped: it is now further from firing than when it was filed, not closer.

**Nine triggers confirmed *not* fired**, so grooming can trust them: `<chrono>` out of
`Log.hpp` · `TE_LOG_ACTIVE_LEVEL` above Info (no CI leg sets it) · Allocators ·
[[File Access — Design]]'s two entries (S5-T3 has not started, and the note is untouched) ·
`FETCHCONTENT_UPDATES_DISCONNECTED` (`cmake/deps.cmake` has not moved since #46, `ed24ae1`) ·
`App::run()` is untestable (`App.cpp` untouched since #63) · the `wait()`-from-worker test
(#64 added `engine/testing/…/AssertCapture.hpp`, but it carries no deadline) · the four
cache-starved CI legs (still one open PR per merge).

**Checked and deliberately not filed.**

- **The private-plumbing-gate entry's rename clause still cannot fire from #64.** Its trigger is
  "a third gate, or the next rename that crosses one of the existing patterns". #64 did move
  `AssertCapture.hpp` between directories, but the three `check()` patterns are
  `TE_LOG_PRIVATE_`, `\binternal::log` and the Tracy identifiers (`.github/workflows/ci.yml:90`,
  `:91`, `:96-98`), and the move touches none of them. The Aug-31 sweep's finding that the
  first clause was satisfied at birth is unchanged and still needs a re-score, not a sweep.
- **Frame pacing (`#prio/high`) has a trigger nobody can evaluate.** *"The first build that runs
  unattended"* reads two ways — a shipped binary left running without a developer, or CI running
  a built exe — and S5-P4 is about to make the second one true. Same class as the Aug-31 sweep's
  unreadable trigger, and re-writing a readiness condition is grooming rather than a sweep, so
  it is flagged here and left alone.
- **`origin/claude/wizardly-cannon-5ymk17` exists on the remote and is exactly `master`'s tip**,
  zero commits ahead. It is a harness artifact of this lane's own session, not work. Harmless,
  but it is a second branch nobody cut deliberately, and the `delete_branch_on_merge` entry
  already notes that nothing prunes these.

### #66, re-read

Head `9e829c6`, unchanged since the PR body edit on Sep 2 at 13:12 UTC. Ten checks green —
both Windows Debug/Release, `win ASan`, both Linux legs, UBSan, TSan, `clang-format`,
`no-code scope` and `diff coverage` — plus `matrix.context` `skipped`, which is
[[B3 — Build & Testing Notes]]'s docs-only stand-in declining a code PR. Nothing to carry
forward except the review itself.

One thing worth recording while the PR is fresh: its branch is `S5-P2/log-level-fallback`, so
the card-ID link ADR-012 § *Consequences* depends on is intact. That is the first correctly
prefixed branch after the three consecutive misses the *guard the branch-name link* entry was
filed for.

### Environment

Nothing needed repair. The `docs/` symlink and `wayland-scanner` were both present on arrival.
Step 1's re-anchor ran on both repos and both report `is-shallow-repository: false` afterwards,
so `01ed7a30` resolving is a real result rather than an artefact of a truncated clone.

### Unverified

- **Nothing was compiled.** No engine code changed, so the build carve-out was not exercised.
  This run proves nothing about `linux-debug`, `ctest` or the toolchain, and it never touches
  Windows or the sanitizer legs.
- **#66's CI is read from GitHub's check runs, not reproduced.** The run it reports is the one
  from Sep 2; nothing re-ran, because nothing was pushed.
- **The sweep covered [[Backlog]] triggers only.** It is not a freshness check and says nothing
  about whether any design note has drifted from the code.
- **Finding 2 marks a trigger as fired; it does not claim grooming missed it.** A ceremony may
  have read the entry and left it, and a run cannot tell the two apart from the file.
- **The "nine not fired" list is a read of each entry against the tree**, at the depth the entry
  itself names. It is not an audit of whether each entry is still worth having.
- **The Dashboard stamp was not advanced**, and this fire had no range to check anyway.

### Cost

| | |
|---|---|
| **CI** | 0 billed minutes. No PR opened, no engine commit, and the vault takes none. |
| **Review owed** | ~15 min, almost all of it #66. Three [[Backlog]] lines to skim on top. |

## 14:16 (Lisbon) — second fire, report-only

### ⚠️ Needs you

- **[#66](https://github.com/techattackteam/TechEngine/pull/66) has now been green and waiting
  on your review for over 24 hours, and it is the only thing that refills this lane.** Head
  still `9e829c6`, `mergeable_state: clean`, nothing moved since the morning fire read it.
  Merging it closes S5-P2; leaving it open means tomorrow's fires have no card again.
- **A [[Backlog]] entry has been describing deleted code for five days and no mechanism would
  ever have caught it.** The `App.cpp` citation entry defers to a `TODO(S3-T13)` block that #65
  deleted hours after the entry was filed. This morning's trigger sweep passed over it *by
  design* — its trigger was already marked fired, and a sweep reads unfired triggers. Entry
  corrected, and the gap filed as its own `#prio/low`.
- **The routine is still running the four-fire prompt text**, five days after `1f4ebfb` changed
  the vault's copy to two. That is the already-carded `#prio/medium` prompt-propagation entry,
  and this fire is fresh evidence its trigger is still live rather than a new finding.
- **No PR, and the day's budget is still untouched.** No 🤖 Auto card is takeable, for the same
  reasons the morning fire gave, so this fire re-ran the mechanical half of S5-P3's sweep at
  the tip the earlier sweep never saw.

### What ran

| | |
|---|---|
| **Card** | *No 🤖 Auto card takeable* — re-resolved S5-P3's citation sweep at `40f7171` |
| **Outcome** | Report only. One [[Backlog]] entry corrected, one added. No artifact edited, no engine file touched. |
| **PR** | None opened. #66 was re-read, **not** merged, and auto-merge was **not** enabled. |
| **CI** | #66 unchanged and green — 11 checks, same run from Sep 2. Nothing re-ran. |
| **Local build** | **Not run.** No engine code changed, so step 6 had nothing to prove. |

### Why this and not the two fallbacks

Both of step 4's fallbacks were spent by this morning's fire. `origin/master` is still
`40f7171`, so the freshness check has no range it has not already covered, and the trigger
sweep ran at 09:13 today.

What was **not** spent: S5-P3's sweep resolved its ~120 citations against `b627332`, and
`master` has since moved two commits that deleted files and moved one. So the mechanical half
of that card was due a re-run at the new tip, and it is the same open 🤖 Auto card rather than
improvised scope.

### The re-resolution — a clean negative result

Every `path:line` citation in the vault was extracted and resolved against `40f7171`, with the
v1 ones resolved against the frozen `v1-reference` tag: **208 occurrences, 142 distinct sites.**

| | |
|---|---|
| Resolve at `40f7171` | all v2 sites except the four below |
| Resolve at `v1-reference` | **all ~50 v1 sites**, unchanged from the Sep-1 result |
| Out of range | `App.cpp:82` · `App.cpp:94` · `App.cpp:95` (the file is 55 lines) |
| Unresolvable by construction | `spdlog/CMakeLists.txt:91-104`, which lives in the FetchContent build tree |

**#64 and #65 rotted no citation.** Every site the earlier sweep validated at `b627332` still
resolves at `40f7171`, and the four exceptions are all already filed — the three `App.cpp` ones
under the `#prio/medium` dangling entry, and `App.cpp:94` under the `#prio/medium` snapshot
entry, whose sha-anchor fix repairs the dangling pointer as a side effect of anchoring the
claim. That is worth knowing before S5-P3 is re-scored: **its mechanical half is stable, and
what is left of the card is the two judgement calls it correctly refused to make.**

### Findings

**1. The `App.cpp` citation entry defers to a block that no longer exists** (corrected in
place, no new entry). It reads "the define it used survives orphaned at
`engine/app/CMakeLists.txt:8-11`, still under its `TODO(S3-T13)`" and defers re-pointing to
S5-T5 on the grounds that S5-T5 deletes it. #65 deleted `TE_DEMO_ASSETS_DIR`, the TODO and
`engine/app/assets/` on Sep 1; `git grep` at `40f7171` returns **no hit for either symbol**.
The deferral still stands on its other leg, because S5-T5 moves the composition root and the
line the artifacts should point at does not exist yet, so the entry is corrected rather than
re-scored.

**2. A fired trigger means the entry is never looked at again** (`#prio/low`, § *etc*, new).
This is the mechanism behind finding 1 and it is the more useful half. A sweep reads unfired
triggers, because a fired one has nothing left to decide; grooming reads a fired entry when it
pulls it. Between those two moments nothing re-resolves what the entry cites. #65 invalidated
**two** entries within hours of their filing, and only the one whose trigger was still unfired
was caught — this morning, and by the sweep that skipped the other. Scored `low` because the
cost is bounded (grooming re-reads the code before acting on it) and the fix is one line of
scope: a sweep re-resolves a fired entry's citations, and only skips re-deciding its trigger.

### Environment

Nothing needed repair. The `docs/` symlink and `wayland-scanner` were both present on arrival.
Step 1's re-anchor ran on both repos and both report `is-shallow-repository: false` afterwards,
so the citations above resolve against complete histories in both the engine repo and its
`v1-reference` tag.

One aside, correcting this morning's: `origin/claude/wizardly-cannon-5ymk17` is **gone** from
the remote, and a new session branch has replaced it. Something does prune them, so the morning
note's link to the `delete_branch_on_merge` entry reads more darkly than the facts support.

### Unverified

- **Nothing was compiled.** No engine code changed, so the build carve-out was not exercised.
  This run proves nothing about `linux-debug`, `ctest` or the toolchain, and never touches
  Windows or the sanitizer legs.
- **The re-resolution is mechanical: file exists, line is in range.** It does **not** re-read
  whether each sentence still describes what its line holds — that is the semantic half the
  Sep-1 report assigned to `/weekly-review`, and it was not repeated here.
- **Its extraction is a regex over backtick-quoted `path:line` spans.** A citation written any
  other way is invisible to it, and the 142/208 counts are counts of what the pattern caught.
- **#66's CI is read from GitHub's check runs, not reproduced.** The run reported is Sep 2's.
- **Finding 2's "caught by nothing" is read from how the two mechanisms are scoped**, plus two
  observed instances. It is not proof that no third mechanism would have found it eventually.

### Cost

| | |
|---|---|
| **CI** | 0 billed minutes. No PR opened, no engine commit, and the vault takes none. |
| **Review owed** | ~15 min, almost all of it still #66. Two [[Backlog]] lines to skim on top. |
