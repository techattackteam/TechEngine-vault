# 🤖 Auto Run — 2026-09-04

> Written by an unattended cloud routine ([[Autonomous Lane — Design]]). One note per day;
> each fire appends a section headed with its local time.

## 09:14 (Lisbon) — first fire of the day

### ⚠️ Needs you

- **The `const FileAccess&` read-only convention is broken, and it was the whole mitigation for
  dropping `FileWriteAccess`.** `copy`, `move` and `rename` shipped `const` while writing to
  disk. [[File Access — Design]] argues that holding a `const FileAccess&` is what a caller who
  must not write can do instead of having a type that forbids it. Three writing methods now go
  straight through that reference. Filed `#prio/medium`, not fixed — the fix is one word per
  signature but it is a decision, and `createDirectory` and `remove` shipped non-const in the
  same card, so the split is already inconsistent.
- **`FileAccess::remove` resolves through `resolveForCreate`**, which is the exact bug S5-T3's
  review caught and corrected for `copy`, `move` and `rename`. `remove`'s path must already
  exist for the same reason and did not move with them, so a file in a lower-priority mount
  returns `NotFound` while `read` finds it. `copy` got an overlay test case; `remove` has none.
  Filed `#prio/medium`. Both readings are defensible, which is why it is not fixed here.
- **S5-P2 is still sitting in the board's To Do column and #66 merged yesterday at 17:40 UTC.**
  Known Issues D1 is gone and the code is on `master`, so the card is done in everything but the
  board. It makes the Auto lane look fuller than it is.
- **The Auto lane is empty.** With S5-P2 landed and S5-P3 down to the two judgement calls it
  correctly refuses, no 🤖 Auto card has takeable work. Same shape S5-P1 recorded, and
  `/sprint-plan` is not until Sep 12-13.
- **No PR, so the day's budget is untouched** and a later fire can still take one if a card
  appears.

### What ran

| | |
|---|---|
| **Card** | *No 🤖 Auto card takeable* — ran the step 4 freshness check over `40f7171..84181fa` |
| **Outcome** | Report only. Three [[Backlog]] entries added, one corrected. No artifact edited, no engine file touched. |
| **PR** | None opened. #66 was read and found already merged. |
| **CI** | Nothing running. No PR opened and no engine commit pushed. |
| **Local build** | **Not run.** No engine code changed, so step 6 had nothing to prove. |

### Why the freshness check, and why it was genuinely due

`origin/master` moved from `40f7171` to `84181fa` after yesterday's fires read it, so this run
had a range no drift check had covered: `0ac1a9b` (#66), `a790890` (#67, S5-T1) and `84181fa`
(#68, S5-T3). Both of yesterday's fires declined the freshness check on the grounds that the
whole range was already covered, and that was true when they ran.

Part of the range is reconciled already. S5-T1 and S5-T3 closed attended on Sep 3, and that
close filed four [[Backlog]] entries and rewrote three sections of [[File Access — Design]]. So
this check looked for what a card close would not have caught, which is why three of its four
findings sit in the half of a note the close did not touch.

**#66 introduced no drift.** Neither [[Logger — Design]] nor
[[ADR-011 — Diagnostics (Logger & Assert)]] names `TE_LOG_ACTIVE_LEVEL`'s unlinked fallback
anywhere, so nothing described the `TRACE` default the commit replaced. Clean negative.

### What changed

- **[[Backlog]] only.** Three entries added under § *platform*, one corrected in place. No
  artifact was edited, no engine file was touched, and no branch was cut.

### Findings

Four. Three are new [[Backlog]] entries, one is a correction to an existing one.

**1. `copy`, `move` and `rename` are `const` and write to disk** (`#prio/medium`, § *platform*,
new). Declared at `engine/platform/include/TechEngine/platform/files/FileAccess.hpp:43-47`.
[[File Access — Design]] § *Why the write split was dropped* names this convention as the entire
replacement for the `FileWriteAccess` type S4-T7 dropped, and § *The read surface* repeats it.
[[Project — Design]]'s `Project::load(const FileAccess&, …)` (`:308`) is built on the same
premise. So is the note's reversal trigger, which calls a future extraction mechanical on the
grounds that `write` and `resolveForCreate` are the only two functions that would move — it is
seven now. Nothing misbehaves today because no consumer holds a `const FileAccess&` it should
not be able to write through; the SDK boundary is where that stops being true.

**2. `remove` resolves a must-exist path through `resolveForCreate`** (`#prio/medium`,
§ *platform*, new). `engine/platform/src/files/FileAccess.cpp:190`. S5-T3's review moved `copy`,
`move` and `rename` sources to `resolveExisting` precisely because `resolveForCreate` takes the
top mount and never probes; `remove`'s argument has the same requirement and was left behind.
[[Project — Design]] § *The five mutating calls* states the sources rule for those three by name
and is silent on `remove`, so the note does not settle it either. Filed rather than fixed
because a delete reaching through an overlay into a lower, possibly read-only mount is a
defensible thing to forbid, and choosing is Miguel's.

**3. [[File Access — Design]]'s header and Decided table are a version behind its own body**
(`#prio/low`, § *platform*, new). § *The write surface* records the five calls shipping at
S5-T3, and the **Status:** line and the Decided table's *Surface* row still call them M3 work.
A reader who stops at the summary table gets the pre-S5-T3 surface. This is the half-updated
shape a card close produces: the section the card argued about was rewritten, the header was
not.

**4. The `move` cross-device entry credits a comment that does not exist** (`#prio/low`,
§ *platform*, corrected). Filed yesterday at S5-T3's close saying `move` was "left undecided at
S5-T3 and carrying a comment saying so". Searching `engine/`, `apps/`, `sdk/` and `cmake/` for
`cross_device`, `cross-device` and `filesystem boundary` returns nothing. Either the comment was
dropped in review under the default-to-no-comment rule or it was never written; either way the
[[Backlog]] entry is now the only record of that decision, and it says so.

**Checked and deliberately not filed.**

- **[[Known Issues]] D3's citation survived #68.** The two comments the commit deleted from
  `MountTable.cpp` are at lines 111 and 122, both below D3's `MountTable.cpp:11`.
- **`executablePath()` has no stale describer.** Every vault mention outside the journal reads
  correctly against the shipped function, including [[File Access — Design]] § *The demo mount is
  throwaway*, which the Sep-3 close had already rewritten to past tense.
- **[[Roadmap]]:37 still lists the mutating half under M3's scope.** That is a milestone
  definition rather than a present-tense claim about the tree, so it is not drift.
- **[[Dashboard]]:34's "`write` the only non-const one" is now false**, but it sits inside the
  dated *Latest advance: 2026-08-30* paragraph, which is a record of what that check found. It
  was true when written.

### Environment

Nothing needed repair. The `docs/` symlink and `wayland-scanner` were both present on arrival.
Step 1's re-anchor ran on both repos and both report `is-shallow-repository: false` afterwards,
so `40f7171..84181fa` is resolved against a complete history rather than a truncated one.

### Unverified

- **Nothing was compiled.** No engine code changed, so the build carve-out was not exercised.
  This run proves nothing about `linux-debug`, `ctest` or the toolchain, and it never touches
  Windows or the sanitizer legs.
- **Findings 1 and 2 are read from the source, not reproduced.** No test was written that
  demonstrates a write through a `const FileAccess&` or a `remove` missing a lower-priority
  mount. Both are read off the declarations and the resolver each call reaches for.
- **Finding 2 assumes `resolveForCreate` still never probes.** That is what
  [[File Access — Design]] § *Resolution* says and what `MountTable.cpp`'s own deleted comment
  said; the function was not re-read line by line this run.
- **The check covers `40f7171..84181fa` only.** Everything before `40f7171` was covered by
  earlier fires and was not re-derived, and the semantic half of S5-P3 — whether each sentence
  still describes what its cited line holds — is not what this was.
- **The Dashboard stamp was not advanced.** Only `/weekly-review` or `/sprint-plan` moves it, so
  these three commits are checked but unstamped.

### Cost

| | |
|---|---|
| **CI** | 0 billed minutes. No PR opened, no engine commit, and the vault takes none. |
| **Review owed** | ~10 min. Four [[Backlog]] lines, and a call on the two `#prio/medium` ones. |

## 14:17 (Lisbon) — second fire, report-only

### ⚠️ Needs you

- **A `#prio/high` entry you are about to pull at `/sprint-plan` points at a blank line.** The
  *Decide `CONVENTIONS.md`'s Error handling row* entry cites `addLogSink` at `Log.hpp:116`, and
  #66 inserted seven lines above it on Sep 3, so the function is at `:123` and `:116` is empty.
  Corrected. Nothing was ever going to catch this: the entry's trigger is already fired, and a
  sweep reads unfired triggers.
- **This morning's clean negative on [[File Access — Design]] § *The demo mount is throwaway*
  was half right, and the wrong half is the one the [[Backlog]] entry was filed for.** S5-T3's
  close rewrote the `executablePath()` sentence into past tense; the two paragraphs above it are
  untouched and still describe `TE_DEMO_ASSETS_DIR`, the `TODO(S3-T13)` and a mount of
  `engine/app/assets/` in the present tense. None of the three exists in the tree.
- **Two triggers had fired against that note and the Sep-3 close carried neither**, though it
  edited the note itself. Same shape as the Sep-3 sweep's `DEPS_PRIVATE platform` finding: the
  change went straight past the entry describing it.
- **No PR, so the day's budget is still untouched** after both fires. No 🤖 Auto card is
  takeable, for the reasons this morning's fire gave, and nothing has changed since.
- **The Auto lane is still empty and `master` has not moved since 09:14.** Tomorrow's fires
  inherit the same problem, and `/sprint-plan` is not until Sep 12-13.

### What ran

| | |
|---|---|
| **Card** | *No 🤖 Auto card takeable* — ran a [[Backlog]] trigger sweep at `84181fa` |
| **Outcome** | Report only. Two triggers marked fired, four entries corrected. No artifact edited, no engine file touched. |
| **PR** | None opened. Nothing to read: #66 merged Sep 3 at 17:40 UTC, before today's first fire. |
| **CI** | Nothing running. No PR opened and no engine commit pushed. |
| **Local build** | **Not run.** No engine code changed, so step 6 had nothing to prove. |

### Why a trigger sweep, and why not the freshness check

The freshness check was spent this morning. `origin/master` is still `84181fa`, unmoved since
the 09:14 fire ran its check over `40f7171..84181fa`, so re-running it would re-derive that
fire's work exactly.

The trigger sweep was genuinely due and had a range the last one never saw. It last ran
**2026-09-03 at 09:13**, against `40f7171`, and #66, #67 and #68 all merged that evening. Two
of those commits satisfy trigger conditions written before them.

### The widened scope, and why it earned itself on the first run

The Sep-3 second fire filed an entry saying a **fired** trigger means nobody re-resolves that
entry's citations again, and gave its trigger as *"the next [[Backlog]] trigger sweep, which is
the run that would carry the change."* This is that run, so the sweep re-resolved the fired
entries too rather than only the unfired ones.

It paid on the first pass. The `Log.hpp:116` rot above sits on a `#prio/high` entry queued for
the next `/sprint-plan`, and it is invisible to the old scope by construction. The entry is
marked fired and now recommends keeping the widened scope; whether it belongs in
`/weekly-review` instead of an ad-hoc sweep is left open, because that is a process decision.

### Findings

Two triggers marked fired, four entries corrected. No new entry was needed.

**1. § *The demo mount is throwaway* — trigger fired, and the repair was half done**
(`#prio/medium`, § *platform*, marked and corrected). Trigger was "S5-T3 or whichever M3 card
first mounts through `project.toml`"; S5-T3 merged Sep 3 as `84181fae` (#68). The Sep-3 close
(`28f24eb`) rewrote `:135` into past tense, which is what this morning's fire saw. `:128-133`
were not touched and still read in the present tense about `TE_DEMO_ASSETS_DIR`, a
`TODO(S3-T13)` in both `App.cpp` and `engine/app/CMakeLists.txt`, and a mount of
`engine/app/assets/`. `grep` across `engine/`, `apps/`, `sdk/` and `cmake/` returns **no hit for
either symbol**, the assets directory is gone, and **no `mount()` call exists outside tests** —
so the section's closing claim that M3 replaces the whole mount set has no mount set to replace.

**2. § *Wiring* — trigger fired, and the edit that fired it went past the section**
(`#prio/low`, § *platform*, marked). Trigger was "the next edit to that note". `28f24eb` edited
the note on Sep 3 and left § *Wiring* untouched, so both of its claims still stand word for
word: `run()` in `App.cpp` as the composition-root owner (it has been the `App` class since
#63), and "`EngineContext` has one field today" (two since `jobs`).

**3. The Error-handling-row entry cites a blank line** (`#prio/high`, § *etc*, corrected).
`Log.hpp:116` → `:123`. #66 inserted a seven-line `NDEBUG` fallback block above `addLogSink`.
Caught only because this sweep re-resolved fired entries.

**4. The quoted-`#include` entry's counts had drifted** (`#prio/medium`, § *etc*, corrected).
"372 of 385" is now **382 of 395**; #67 and #68 added ten include lines, all angle-bracketed.
The substance is unchanged: still exactly **13** quoted includes, still the same sites, still
all from #62 and #63.

**5. The branch-name guard cannot be an equality test** (`#prio/medium`, § *etc*, corrected).
The entry's proposed fix is to compare a merged commit's prefix against the open cards on
[[Sprint Board]]. #65 merged on `S5-T5/demo-mount-removal` while **S5-T5 is still in To Do**,
because that card landed in halves, so a legal commit names an open card. Four cards have been
named correctly since the three consecutive misses — #64, #66, #67, #68. The guard must catch a
prefix matching **no** card, not one matching an open one.

**Checked and deliberately not filed.**

- **Both private-plumbing gates still match something.** `TE_LOG_PRIVATE_` returns 27 hits under
  `engine/base/` and `\binternal::log` returns 3, so the failure mode the entry names — a rename
  emptying the pattern and the gate passing on an empty search — has not happened. #66 touched
  `Log.hpp` but renamed nothing.
- **The `TE_ENSURE` report-once trigger has not fired.** No `TE_ENSURE` was added anywhere in
  `40f7171..84181fa`; the three new assertions are all `TE_CHECK`.
- **Four triggers whose witness the three commits never touched:**
  `FETCHCONTENT_UPDATES_DISCONNECTED` (`cmake/deps.cmake` unmoved), `App::run()` is untestable
  (`App.cpp` unmoved), the `wait()`-from-worker test (still no deadline-capable helper), and
  `.gitattributes` (the tree still carries no committed data asset).
- **The four cache-starved CI legs are still not fired.** Its trigger is more than one open PR
  per master merge. #66, #67 and #68 were strictly sequential on Sep 3 — each opened after the
  previous merged (17:40, 18:21, 19:07), so nothing overlapped.
- **Every citation this morning's fire filed resolves.** `FileAccess.hpp:43-47`,
  `FileAccess.cpp:190`, `FileAccess.cpp:262`, `FileAccessTests.cpp:526` and
  `ExecutablePath.cpp:16-19` were each re-read and each holds what its entry says it holds.
- **The `TE_LOG_ACTIVE_LEVEL`-above-Info entry is intact.** `engine/base/CMakeLists.txt:18` and
  `MathFormatTests.cpp:47` both still resolve, and #66 did not touch `engine/base/CMakeLists.txt`.
- **The snapshot-citation entry's own description is still accurate.**
  `engine/base/CMakeLists.txt:15` is `LIBS_PRIVATE spdlog::spdlog)` and `:4` is a source line,
  exactly as the entry says. It wants a sha-anchoring convention, not a repoint.

### Environment

Nothing needed repair. The `docs/` symlink and `wayland-scanner` were both present on arrival.
Step 1's re-anchor ran on both repos and both report `is-shallow-repository: false` afterwards.

### Unverified

- **Nothing was compiled.** No engine code changed, so the build carve-out was not exercised.
  This run proves nothing about `linux-debug`, `ctest` or the toolchain, and it never touches
  Windows or the sanitizer legs.
- **The sweep covers [[Backlog]] triggers and citations only.** It is not a freshness check and
  says nothing about whether a design note has drifted beyond the four entries above.
- **Finding 1 is a read of the section against `grep` over the tree**, not a claim about what
  the section should say instead. Rewriting it is a design read and belongs to whoever takes the
  entry.
- **Findings 1 and 2 mark triggers as fired; neither claims the Sep-3 close was wrong to pass
  them over.** A close may have read an entry and left it deliberately, and a run cannot tell
  that from the file.
- **The citation re-resolution is mechanical: the file exists and the line holds what the entry
  quotes.** It is not a re-reading of whether each entry is still worth having.
- **The Dashboard stamp was not advanced.** `master` has not moved since this morning, and only
  `/weekly-review` or `/sprint-plan` moves the stamp.

### Cost

| | |
|---|---|
| **CI** | 0 billed minutes. No PR opened, no engine commit, and the vault takes none. |
| **Review owed** | ~5 min. Five [[Backlog]] lines, all corrections rather than decisions. |
