# 04 Systems — v2

Living docs per v2 subsystem/utility. **Filled as each is built** — keep terse
(see `CLAUDE.md` → token economy).

- **`Utilities/`** — helper facilities (Logger, Profiler, Assert, Clock, …). One doc each.
- **Systems** (ECS, resources, renderer, …) — one doc each at the top level, as they're built.
- Design-doc shape: [[Design Doc Template]]. **ADR = the irreversible decision; the design doc = the _how_.**
- v1 subsystems: see [[v1 Code Audit]] (reference prototype).
