# 04 Systems — v2

Living docs per v2 subsystem. **Empty on purpose** — fill as each system is built.

- v1 subsystems: see [[v1 Code Audit]] (reference prototype).
- One note per system; keep terse (see `CLAUDE.md` → token economy).
