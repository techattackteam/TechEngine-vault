# 2026 Q3 — Foundation & Direction

**Theme:** plan the fresh start, then build v2 on solid ground.

Direction is set ([[ADR-004 — Fresh start (v2) with v1 as reference]]): the
current code becomes a **reference prototype (v1)** and the engine is rebuilt
fresh, planned first. Q3 turns that into concrete architecture ADRs and the first
buildable slice of v2.

## Q3 objectives

1. **Process live** — vault, ADR flow, and sprint rhythm actually used weekly.
2. **v2 foundation designed** — architecture, renderer approach, and build/testing
   baseline captured as Accepted ADRs; salvage list from v1 written.
3. **v2 building** — `v1-reference` frozen; a `base` you can trust, and the
   [[Roadmap]] chain opened.

## Sprints

| Dates | Sprint | Headline goal |
|-------|--------|---------------|
| Jul 19–26 | [[2026-07 Sprint 01 — Foundation Planning (v2)]] | Audit v1, write v2 foundation ADRs + conventions, set up AI + git flow — ✅ **closed Jul 25** |
| Jul 25 – Jul 31 | [[2026-08 Sprint 02 — Base Foundation]] | **`base` you can trust** — Logger, Assert, Clock, unit-tested, proven by a headless fixed-timestep app loop — ✅ **closed Aug 2**, 4 weeks early |
| Aug 1 – Aug 28 | [[2026-08 Sprint 03 — M1 Enablers]] | **M1's two gates decided, and the vocabulary built** — Profiler + Events ADRs, math, an `IFileSystem` note |
| Aug 29 – Sep 25 | Sprint 04 (planned on the Aug 29–30 boundary) | **M2 ‖ M3** — the threading ADR (unblocked by M1's Profiler) and the project + `projects/dev/` testbed |

## Definition of done for Q3

- [x] v2 architecture + build/testing ADRs **Accepted** — ADRs 005–008. *(Rewritten 2026-07-31:
      the **renderer** ADR left Q3 with C2. It now gates **R1** on the [[Roadmap]], so the clause
      that could never close is gone rather than carried.)*
- [x] [[Lessons from v1 (reference prototype)]] salvage table filled *(2026-07-19)*.
- [x] `v1-reference` tag exists; `v2` is the working mainline that builds + runs *(Jul 24)*.
- [ ] A demo of what Q3 actually built — the **`projects/dev/` testbed running** if Sprint 04
      reaches M3, otherwise **M1 exercised headless under a profiler capture** (Sprint 03's own
      demo line). C2's "first vertical slice" demo is **withdrawn** with the direction change,
      not deferred. *(Re-aimed 2026-08-02: Sprint 03 was re-scoped to M1's gates + leaf
      enablers, so M3 is Sprint 04's reach, not Sprint 03's.)*
